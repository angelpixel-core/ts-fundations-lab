# Pseudocodigo (ES)

1. Crear string acumulador vacio `reversed`.
2. Recorrer `text` desde el ultimo indice hasta el primero.
3. Concatenar cada caracter a `reversed`.
4. Devolver `reversed`.

## Idea clave

Recorrer de derecha a izquierda evita estructuras intermedias innecesarias.
